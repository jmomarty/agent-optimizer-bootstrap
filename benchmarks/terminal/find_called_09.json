{
  "id": "terminal_find_called_09",
  "kind": "terminal_command",
  "split": "dev",
  "goal": "Find a file called quarterly report under /srv/shared.",
  "context": {
    "cwd": "/srv/shared",
    "available_commands": [
      "find",
      "rg",
      "ls"
    ],
    "notes": "Return one shell command only."
  },
  "expected_command": "find /srv/shared -type f \\( -iname '*quarterly*report*' -o -iname 'quarterlyreport' \\)"
}

{
  "id": "terminal_locate_10",
  "kind": "terminal_command",
  "split": "dev",
  "goal": "Locate a directory named backup jobs under /ops and return one shell command.",
  "context": {
    "cwd": "/ops",
    "available_commands": [
      "find",
      "rg",
      "ls"
    ],
    "notes": "Prefer a case-insensitive name match."
  },
  "expected_command": "find /ops -type d \\( -iname '*backup*jobs*' -o -iname 'backupjobs' \\)"
}
